import { BrainCircuit, Download, HeartPulse, ShieldAlert, UserRound, UsersRound } from 'lucide-react';
import { useState } from 'react';
import { PolarAngleAxis, PolarGrid, Radar, RadarChart, ResponsiveContainer, Tooltip } from 'recharts';
import { mockAdvisories } from '../data/mockAdvisories';
import type { AdvisoryCategory } from '../types/heatwave';
import { neoButton, neoCard, neoInput } from '../utils/theme';

const tabs: { category: AdvisoryCategory; label: string; icon: typeof UserRound }[] = [{ category: 'Citizen', label: 'Citizens', icon: UserRound }, { category: 'Worker', label: 'Workers', icon: UsersRound }, { category: 'Elderly', label: 'Elderly & vulnerable', icon: HeartPulse }, { category: 'Health', label: 'Health authorities', icon: ShieldAlert }];
const vulnerabilityByPersona: Record<AdvisoryCategory, { metric: string; value: number }[]> = { Citizen: [{ metric: 'UV Index', value: 88 }, { metric: 'Humidity', value: 48 }, { metric: 'Air Temp', value: 92 }, { metric: 'Solar Radiation', value: 84 }, { metric: 'Dehydration', value: 76 }], Worker: [{ metric: 'UV Index', value: 94 }, { metric: 'Humidity', value: 54 }, { metric: 'Air Temp', value: 96 }, { metric: 'Solar Radiation', value: 90 }, { metric: 'Dehydration', value: 95 }], Elderly: [{ metric: 'UV Index', value: 74 }, { metric: 'Humidity', value: 66 }, { metric: 'Air Temp', value: 91 }, { metric: 'Solar Radiation', value: 70 }, { metric: 'Dehydration', value: 94 }], Health: [{ metric: 'UV Index', value: 82 }, { metric: 'Humidity', value: 58 }, { metric: 'Air Temp', value: 95 }, { metric: 'Solar Radiation', value: 82 }, { metric: 'Dehydration', value: 89 }] };
export default function Advisory() { const [active, setActive] = useState<AdvisoryCategory>('Citizen'); const [temp, setTemp] = useState(42); const [intensity, setIntensity] = useState('Medium'); const rest = temp >= 45 ? (intensity === 'Heavy' ? 30 : 20) : temp >= 40 ? (intensity === 'Heavy' ? 20 : 15) : 10; const advisory = mockAdvisories.filter((item) => item.category === active); return <div className="mx-auto max-w-6xl p-4 md:p-6"><section className="rounded-2xl border border-blue-200 border-l-4 border-l-blue-600 bg-white p-7 shadow-sm"><div className="flex items-center gap-3"><BrainCircuit className="text-blue-600" size={31} /><div><p className="text-sm font-semibold text-blue-700">Decision support</p><h1 className="text-3xl font-black">AI Advisory Center</h1><p className="mt-1 text-slate-600">Actionable heat-safety guidance tailored to each audience.</p></div></div><div className="mt-6 flex flex-wrap gap-2">{tabs.map(({ category, label, icon: Icon }) => <button key={category} onClick={() => setActive(category)} className={`flex items-center gap-2 rounded-xl border px-4 py-2 text-sm font-semibold transition-all duration-300 ${active === category ? 'border-blue-600 bg-blue-600 text-white shadow-md' : 'border-slate-200 bg-white text-slate-600 hover:border-slate-400 hover:bg-slate-50'}`}><Icon size={17} />{label}</button>)}</div></section><section className="my-6 grid gap-5 lg:grid-cols-2">{advisory.map((item) => <article key={item.id} className={`${neoCard} border-t-4 border-t-blue-500 bg-white`}><div className="flex justify-between gap-3"><div><p className="text-sm font-semibold text-blue-700">{item.category} protocol</p><h2 className="mt-1 text-2xl font-bold">{item.title}</h2></div><span className="h-fit rounded-full bg-emerald-100 px-2.5 py-1 text-xs font-bold text-emerald-800">{item.confidence}% confidence</span></div><p className="mt-4 leading-7 text-slate-600">{item.body}</p><ul className="mt-4 space-y-2">{item.actions.map((action) => <li key={action} className="rounded-xl bg-slate-50 p-3 text-sm font-medium text-slate-700">✓ {action}</li>)}</ul><button className={`${neoButton} mt-5 bg-slate-900 text-white`}><Download className="mr-2 inline" size={16} />Download guidance</button></article>)}<article className={`${neoCard} bg-white`}><h2 className="font-bold">Heat vulnerability index</h2><p className="mt-1 text-sm text-slate-500">Current exposure profile for {active.toLowerCase()} guidance.</p><div className="mt-4 h-80"><ResponsiveContainer width="100%" height="100%"><RadarChart data={vulnerabilityByPersona[active]}><PolarGrid /><PolarAngleAxis dataKey="metric" tick={{ fontSize: 11 }} /><Tooltip /><Radar dataKey="value" name="Risk score" stroke="#2563eb" fill="#2563eb" fillOpacity={0.35} /></RadarChart></ResponsiveContainer></div></article></section><section className={`${neoCard} bg-white`}><h2 className="font-bold">Work-rest cycle calculator</h2><p className="mt-1 text-sm text-slate-500">Use heat and job intensity to estimate a shaded recovery interval per hour.</p><div className="mt-4 grid gap-4 md:grid-cols-3"><label className="text-sm font-semibold">Temperature °C<input className={`${neoInput} mt-1 w-full`} type="number" value={temp} onChange={(event) => setTemp(Number(event.target.value))} /></label><label className="text-sm font-semibold">Job intensity<select className={`${neoInput} mt-1 w-full`} value={intensity} onChange={(event) => setIntensity(event.target.value)}><option>Light</option><option>Medium</option><option>Heavy</option></select></label><div className="rounded-xl border border-amber-200 bg-amber-50 p-4"><p className="text-sm font-semibold text-amber-800">Recommended cool-down</p><p className="mt-1 text-3xl font-black text-amber-950">{rest} min/hour</p></div></div></section></div>; }
