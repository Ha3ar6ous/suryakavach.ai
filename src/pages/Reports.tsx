import { Area, AreaChart, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { Camera, CheckCircle2, MapPin, Send } from 'lucide-react';
import { useState, type FormEvent } from 'react';
import { mockReports, mockTrends } from '../data/mockReports';
import type { IncidentReport, RiskLevel } from '../types/heatwave';
import { getRiskColor, neoButton, neoCard, neoInput } from '../utils/theme';
const symptomOptions = ['Dizziness', 'Headache', 'Fatigue', 'Nausea', 'Heat cramps']; const categories = [{ name: 'Heatstroke', value: 27, color: '#dc2626' }, { name: 'Dehydration', value: 35, color: '#f59e0b' }, { name: 'Outdoor exhaustion', value: 24, color: '#2563eb' }, { name: 'Cardiovascular distress', value: 14, color: '#8b5cf6' }];
export default function Reports() { const [city, setCity] = useState(''); const [symptoms, setSymptoms] = useState<string[]>([]); const [score, setScore] = useState(50); const [reports, setReports] = useState<IncidentReport[]>(mockReports); const severity: RiskLevel = score >= 80 ? 'Extreme' : score >= 60 ? 'Severe' : 'Moderate'; function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (!city.trim() || !symptoms.length) return; setReports((items) => [{ id: `local-${Date.now()}`, city, symptoms, stressScore: score, severity, timestamp: new Date().toISOString(), awsVerified: false }, ...items]); setCity(''); setSymptoms([]); setScore(50); } function toggle(symptom: string) { setSymptoms((items) => items.includes(symptom) ? items.filter((item) => item !== symptom) : [...items, symptom]); } return <div className="mx-auto max-w-7xl p-4 md:p-6"><section className="grid gap-5 lg:grid-cols-2"><form onSubmit={submit} className={`${neoCard} border-t-4 border-t-red-500 bg-white`}><p className="text-sm font-semibold text-red-700">Community network</p><h1 className="mt-1 text-2xl font-black">Report heat stress</h1><p className="mt-2 text-slate-600">Your report helps identify growing risks and support local response.</p><label className="mt-5 block text-sm font-semibold">City<input required value={city} onChange={(event) => setCity(event.target.value)} className={`${neoInput} mt-1 w-full`} placeholder="Your city" /></label><p className="mt-4 text-sm font-semibold">Symptoms</p><div className="mt-2 flex flex-wrap gap-2">{symptomOptions.map((symptom) => <button key={symptom} type="button" onClick={() => toggle(symptom)} className={`rounded-lg border px-3 py-2 text-sm font-medium transition ${symptoms.includes(symptom) ? 'border-red-500 bg-red-50 text-red-800' : 'border-slate-200 bg-white text-slate-600 hover:border-slate-400'}`}>{symptom}</button>)}</div><label className="mt-4 block text-sm font-semibold">Heat stress rating: {score}/100<input className="mt-2 w-full accent-red-600" type="range" min="20" max="100" value={score} onChange={(event) => setScore(Number(event.target.value))} /></label><div className="mt-3 flex items-center gap-2 text-sm text-slate-600"><Camera size={18} />Optional photo attachment (mock)</div><button className={`${neoButton} mt-5 bg-red-600 text-white`}><Send className="mr-2 inline" size={16} />Submit report ({severity})</button></form><div className="grid gap-5"><article className={`${neoCard} bg-white`}><h2 className="font-bold">Incident category distribution</h2><div className="mt-3 h-60"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={categories} dataKey="value" nameKey="name" outerRadius={82}>{categories.map((entry) => <Cell key={entry.name} fill={entry.color} />)}</Pie><Tooltip /></PieChart></ResponsiveContainer></div></article><article className={`${neoCard} bg-white`}><h2 className="font-bold">Community trend · last 30 days</h2><div className="mt-3 h-60"><ResponsiveContainer width="100%" height="100%"><AreaChart data={mockTrends}><XAxis dataKey="month" /><YAxis /><Tooltip /><Area type="monotone" dataKey="reports" stroke="#dc2626" fill="#fecaca" strokeWidth={2} /></AreaChart></ResponsiveContainer></div></article></div></section><section className={`${neoCard} mt-6 bg-white`}><h2 className="font-bold">Live community report feed</h2><div className="mt-4 grid gap-3 md:grid-cols-2">{reports.map((report) => <article key={report.id} className="rounded-xl border border-slate-200 p-4 transition hover:border-slate-400"><div className="flex flex-wrap items-center justify-between gap-2"><h3 className="flex items-center gap-1 font-bold"><MapPin size={17} />{report.city}</h3><span className={`rounded-full border px-2.5 py-1 text-xs font-bold ${getRiskColor(report.severity)}`}>{report.severity}</span></div><p className="mt-2 text-sm text-slate-600">{report.symptoms.join(' · ')}</p><div className="mt-3 flex items-center justify-between text-xs font-medium text-slate-500"><span>Stress score {report.stressScore}/100</span><span className={report.awsVerified ? 'text-emerald-700' : 'text-slate-500'}>{report.awsVerified ? <><CheckCircle2 className="mr-1 inline" size={14} />AWS verified</> : 'Verification pending'}</span></div></article>)}</div></section></div>; }
