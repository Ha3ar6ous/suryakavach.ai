import { BrainCircuit, ChevronDown, Database, Radio, ShieldCheck, Telescope } from 'lucide-react';
import { useState } from 'react';
import { neoCard } from '../utils/theme';
const layers = [{ title: 'Data acquisition', text: 'IMD historical grids and real-time AWS observations form a local source of truth.', icon: Database }, { title: 'Analytics', text: 'Spatio-temporal segmentation reveals regional heat patterns.', icon: Telescope }, { title: 'AI intelligence', text: 'Forecasting and severity classification surface early warnings.', icon: BrainCircuit }, { title: 'Forecast validation', text: 'Predictions are compared with IoT weather-station reality.', icon: ShieldCheck }, { title: 'Decision support', text: 'LLM advisories translate risk evidence into clear actions.', icon: Radio }];
const faqs = [{ q: 'How accurate is the AI model?', a: 'The live model target is 94.8% forecast accuracy, continuously validated against AWS observations.' }, { q: 'What should I do during an extreme heat alert?', a: 'Avoid peak sun, hydrate consistently, check vulnerable neighbours, and use a cooling centre when indoor heat cannot be managed.' }, { q: 'Where does the data come from?', a: 'suryakavach.ai combines IMD-aligned historical climate datasets with IoT automated weather-station telemetry.' }];
export default function About() { const [open, setOpen] = useState<number | null>(null); return <div className="mx-auto max-w-6xl p-4 md:p-6"><section className="rounded-2xl border border-blue-200 border-l-4 border-l-blue-600 bg-white p-8 shadow-sm"><p className="text-sm font-semibold text-blue-700">Research-led climate intelligence</p><h1 className="mt-1 text-3xl font-black">Built for heat resilience</h1><p className="mt-3 max-w-3xl text-lg leading-8 text-slate-600">suryakavach.ai connects meteorological science, ground sensors, machine learning, and clear public guidance to help communities prepare for dangerous heat.</p></section><section className={`${neoCard} my-6 bg-white`}><h2 className="text-xl font-bold">Five-phase architecture</h2><div className="mt-5 grid gap-3 md:grid-cols-5">{layers.map(({ title, text, icon: Icon }, index) => <div key={title} className="rounded-xl border border-slate-200 p-4 transition-all duration-300 hover:-translate-y-1 hover:border-slate-400 hover:shadow-lg"><span className="text-sm font-semibold text-blue-700">0{index + 1}</span><Icon className="mt-4 text-slate-700" size={25} /><h3 className="mt-3 font-bold">{title}</h3><p className="mt-2 text-sm leading-6 text-slate-600">{text}</p></div>)}</div></section><section className={`${neoCard} mb-6 bg-white`}><h2 className="font-bold">Research & collaboration</h2><div className="mt-4 grid gap-3 md:grid-cols-3">{['IMD-aligned meteorological research frameworks', 'IoT AWS deployment and field validation', 'Applied AI & Data Science decision support'].map((item) => <div key={item} className="rounded-xl bg-slate-50 p-4 text-sm font-medium text-slate-700">{item}</div>)}</div></section><section className={`${neoCard} bg-white`}><h2 className="text-xl font-bold">Frequently asked questions</h2><div className="mt-4 space-y-3">{faqs.map((faq, index) => <div key={faq.q} className="rounded-xl border border-slate-200"><button onClick={() => setOpen(open === index ? null : index)} className="flex w-full items-center justify-between p-4 text-left font-semibold"><span>{faq.q}</span><ChevronDown className={open === index ? 'rotate-180 transition-transform' : 'transition-transform'} /></button>{open === index && <p className="border-t border-slate-200 p-4 leading-7 text-slate-600">{faq.a}</p>}</div>)}</div></section></div>; }
